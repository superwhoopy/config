# vim: set ft=sh:

. ../.envrc

{ _iswin && _venv_sh="../.venv/Scripts/activate"; } || \
  _venv_sh="../.venv/bin/activate"

# Python virtual env
. ${_venv_sh}

alias pytest="pytest --t32-exe='${T32}' --bootloader='${BOOTLOADER}'"
alias pythaast="notify pytest --haas=${HAAS_BOARDS}"
